structure person :
	text name
	text surname
	time birthdate
	symbol sex
	
interactor Gauge :
	actor user
	number flow value from parent to user
	
	
interactor ControlPanel :
	actor user
	actor system
	
	Gauge theGauge
	
	symbol flow selection to user
	symbol event select from user
	void event increment from user to system
	void event decrement from user to system
	void event reset from user to system
	number flow min from system
	number flow max from system
	number flow x from system
	number flow y from system
	number flow current
	
	on init : 	always current = 
					switch(bobie) {
						case 10 : 3 
						default : 5
						}
				always value to theGauge = if(a==5) 30 else 40
				
	on select if true : set selection = if((5 > (if(true) 4 else 3)) && false) 1+1*(switch(4) {case 3:2 default:1}) else 6
		
				
	
	